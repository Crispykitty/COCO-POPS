glob { x y result }
proc { }
func { }
main {
    var { a b }
    x = 10;
    y = 5;
    
    result = ( x mult y );
    print result;
    
    result = ( x div y );
    print result;
    
    a = ( neg x );
    print a;
    
    if ( ( x > 0 ) and ( y > 0 ) ) {
        print "both positive";
    };
    
    b = 0;
    if ( not ( b eq 0 ) ) {
        print "not zero";
    } else {
        print "is zero";
    };
    
    halt
}
