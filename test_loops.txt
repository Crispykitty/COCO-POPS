glob { x }
proc { }
func { }
main {
    var { counter }
    counter = 5;
    while ( counter > 0 ) { 
        counter = ( counter minus 1 ); 
        print counter;
    };
    do { 
        print "loop"; 
        counter = ( counter plus 1 ); 
    } until ( counter eq 5 );
    halt
}